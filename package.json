{
  "name": "uninus-greenhouse-rollup-card",
  "version": "1.3.0",
  "description": "Standalone Home Assistant Lovelace card for greenhouse roll-up covers",
  "type": "module",
  "main": "uninus-greenhouse-rollup-card.js",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build && node --check uninus-greenhouse-rollup-card.js",
    "ci": "npm run check && git diff --check"
  },
  "keywords": [
    "home-assistant",
    "lovelace",
    "hacs",
    "greenhouse",
    "rollup",
    "custom-card"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.6",
    "linkedom": "^0.18.13"
  }
}
